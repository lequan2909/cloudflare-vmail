---
import Star from '@/components/Star.astro'
import DocsLayout from '@/layouts/DocsLayout.astro'

const Env = Astro.locals.runtime.env
---

<DocsLayout
  title={`About our service - ${Env.SITE_NAME}`}
  description={`About ${Env.SITE_NAME}`}
  pageTitle={`About ${String(Env.SITE_NAME)}`}
  lastUpdated="2024-03-15"
>
  <div class="mb-8">
    <div class="bg-gradient-to-br from-primary/10 via-primary/5 to-transparent rounded-2xl p-8 border border-primary/20">
      <p class="text-lg text-foreground/90 leading-relaxed mb-6">
        <strong>{Env.SITE_NAME}</strong> is a privacy-focused virtual temporary email service that
        requires no registration. Get temporary email addresses without revealing any personal information,
        greatly protecting your privacy and keeping your real email safe from spam.
      </p>
      <div class="flex items-center gap-2">
        <Star />
        <span class="text-sm text-muted-foreground">
          Support our work by giving us a star on GitHub
        </span>
      </div>
    </div>
  </div>

  <h2 id="key-features">Key Features</h2>

  <h3 id="privacy-enhanced">🔒 Privacy Enhanced</h3>
  <p>
    Get temporary email addresses without revealing any personal information, greatly
    protecting your privacy and keeping your real email safe from spam.
  </p>

  <h3 id="multiple-domains">🌐 Multiple Domains</h3>
  <p>
    Choose from multiple domain names, making it convenient for you to use in different
    scenarios and applications.
  </p>

  <h3 id="cloudflare-powered">⚡ Cloudflare Powered</h3>
  <p>
    100% running on the <strong>Cloudflare</strong> network, providing you with super-fast performance
    and global availability. We use Cloudflare D1 to store email data, ensuring security and reliability.
  </p>

  <h3 id="secure-storage">🛡️ Secure Storage</h3>
  <p>
    We use Cloudflare D1 to store email data, ensuring security and reliability. You can check
    our <a href="/privacy">Privacy Policy</a> for more details.
  </p>

  <h2 id="responsible-usage">Responsible Usage</h2>
  <div class="not-prose">
    <div
      class="bg-amber-50 dark:bg-amber-950/20 border border-amber-200 dark:border-amber-800 rounded-lg p-6 my-6"
    >
      <div class="flex items-start gap-3">
        <svg class="h-6 w-6 text-amber-600 dark:text-amber-500 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
        </svg>
        <div>
          <p class="font-semibold text-amber-900 dark:text-amber-200 mb-2">Important Notice</p>
          <p class="text-amber-800 dark:text-amber-300 text-sm leading-relaxed">
            Misuse of the temporary email service not only violates our
            terms of service but can also impact the normal use of other users. We encourage each user
            to use our services responsibly to ensure that resources are allocated and used appropriately.
          </p>
        </div>
      </div>
    </div>
  </div>
</DocsLayout>
